---
import Base from '../../layouts/Base.astro';
import { t } from '../../i18n';

const locale = 'en';
---

<Base locale={locale} pageKey="about">
  <div class="page">
    <h1 class="page__heading">{t('pages.about.heading', locale)}</h1>
    <p class="page__intro">{t('pages.about.intro', locale)}</p>
  </div>
</Base>
